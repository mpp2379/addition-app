# Addition App

Simple Flask application that adds two numbers using pandas.

## Run

```bash
python -m venv venv
# Windows
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```
Open http://127.0.0.1:5000
